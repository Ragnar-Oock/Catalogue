<form method="post" id="delete-fond" action="{{ path('fond_delete', {'id': fond.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer ce fond?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fond.id) }}">
    {% if noBtn is not defined %}
        <button class="btn">Delete</button>
    {% endif %}
</form>
