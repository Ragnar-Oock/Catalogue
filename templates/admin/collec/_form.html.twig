{{ form_start(form, {'attr': {'id': 'edit-collec"'}}) }}
    {{ form_widget(form) }}
    {% if noBtn is not defined %}
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    {% endif %}
    
{{ form_end(form) }}
