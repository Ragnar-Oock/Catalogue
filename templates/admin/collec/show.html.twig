{% extends 'base.html.twig' %}

{% block title %}Liste des collections{% endblock %}

{% block body %}
    <h1>Liste des collections</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collec.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ collec.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ collec.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collec_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('collec_edit', {'id': collec.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('admin/collec/_delete_form.html.twig') }}
{% endblock %}
