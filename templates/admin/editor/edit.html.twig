{% extends 'base.html.twig' %}

{% block title %}Modifier un éditeur{% endblock %}

{% block main %}
    <h1>Modifier un éditeur</h1>

    {{ include('admin/editor/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('editor_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('admin/editor/_delete_form.html.twig') }}
{% endblock %}
