{% extends 'base.html.twig' %}

{% block title %}Ajouter un éditeur{% endblock %}

{% block main %}
<div class="fiche">
    <h1 class="fiche__title">Ajouter un éditeur</h1>

    {{ include('admin/editor/_form.html.twig', {'button_label': 'Enregistrer', 'noBtn': true}) }}

    <div class="d-flex">
        <div class="btn-group ml-auto">
            <a href="{{ path('editor_index') }}" class="btn btn-outline-secondary">Annuler</a>
            <button class="btn btn-secondary" form="edit-editor" type="submit">Enregistrer</button>
        </div>
    </div>

</div>

{% endblock %}
