{% extends 'base.html.twig' %}

{% block title %}Modifier un document{% endblock %}

{% block main %}
    <h1>Modifier un document</h1>

    {{ include('admin/document/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('document_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('admin/document/_delete_form.html.twig') }}
{% endblock %}
