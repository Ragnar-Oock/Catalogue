{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block main %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ type.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>

    <a href="{{ path('type_edit', {'id': type.id}) }}" class="btn btn-secondary">Modifier</a>

    {{ include('admin/type/_delete_form.html.twig') }}
{% endblock %}
